--[[	© 2011-2012 CloudSixteen.com do not share, re-distribute or modify	this file without the permission of its owner(s).
	Conna Wiles (connawiles@gmail.com)--]]cursor = {};cursor.m_default = CURSOR_ARROW;cursor.m_current = CURSOR_ARROW;cursor.m_stored = {};-- A function to add a cursor.function cursor.Add(enumName, fileName)	cursor.m_stored[enumName] = fileName;	_G[enumName] = fileName;end;-- A function to set the default cursor.function cursor.SetDefault(cursorID)	if (cursor.m_current == cursor.m_default) then		cursor.Set(cursorID);	end;		cursor.m_default = cursorID;end;-- A function to set the cursor to its default.function cursor.MakeDefault()	cursor.Set(cursor.m_default);end;-- A function to get the current cursor.function cursor.Get()	return cursor.m_current;end;-- A function to set the current cursor.function cursor.Set(cursorID, origin)	if (cursor.m_current == cursorID) then return; end;		if (type(cursorID) == "number") then		display.Window():SetCursorSys(cursorID);	else		display.Window():SetCursorImg(cursorID, origin or ORIGIN_CENTER);	end;		cursor.m_current = cursorID;end;